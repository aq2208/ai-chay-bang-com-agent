# Production dependencies — what ships in the AgentBase Docker image.
# Local/dev-only extras (FastAPI harness, Streamlit, Gemini/Claude SDKs) live in
# requirements-dev.txt.

# ── AgentBase runtime + LLM (MaaS is OpenAI-compatible) ─────────────────────
greennode-agentbase
openai
python-dotenv

# ── ML / NLP (baked into the image at build time) ──────────────────────────
# torch is installed separately in Dockerfile with the CPU-only wheel;
# do NOT list it here unpinned — pip would re-resolve from PyPI and may
# upgrade to a CUDA build or a version incompatible with the models below.
sentence-transformers>=3.0.0,<3.4.0
transformers>=4.40.0,<4.46.0
chromadb


# ── Data connectors ────────────────────────────────────────────────────────
jira
requests
httpx              # used by llm_client.vision() to download image URLs

# ── Database & Web Server ──────────────────────────────────────────────────
fastapi
uvicorn
SQLAlchemy
psycopg2-binary

# ── Misc ────────────────────────────────────────────────────────────────────
pydantic
