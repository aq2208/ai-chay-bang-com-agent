# ── LLM providers (install whichever you use) ────────────────────────────
anthropic          # LLM_PROVIDER=anthropic
google-genai       # LLM_PROVIDER=google  (new SDK — NOT google-generativeai)
openai             # LLM_PROVIDER=openai

# ── Core infrastructure ───────────────────────────────────────────────────
python-dotenv
fastapi
uvicorn[standard]
apscheduler
streamlit
chromadb
pydantic
httpx              # used by llm_client vision() to download image URLs for Gemini

# ── ML / NLP ──────────────────────────────────────────────────────────────
sentence-transformers
transformers
torch

# ── Data connectors ───────────────────────────────────────────────────────
jira
requests
